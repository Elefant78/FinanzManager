using System.Windows;

namespace FinanzManager;

/// <summary>
/// ============================================================
/// MainWindow.xaml.cs — Code-Behind für das Hauptfenster
/// ============================================================
/// In Phase 5 wieder leer: der DataContext wird von App.xaml.cs
/// gesetzt, alle Logik kommt vom MainViewModel via Data-Binding.
/// ============================================================
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
